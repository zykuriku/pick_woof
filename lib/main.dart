import 'package:flutter/material.dart';

void main() => runApp(PickWoof());

class PickWoof extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
